# Never ship these into the build context — they bloat it and can leak secrets.
node_modules
dist
.env
.env.*
*.log
npm-debug.log*

# Test and dev-only material the production image has no use for.
test
coverage
.vscode
Dockerfile
.dockerignore
